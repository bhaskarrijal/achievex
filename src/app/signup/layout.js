import React from 'react'


export const metadata = {
    title: 'Sign Up - AchieveX',
}

const layout = ({ children }) => {
    return (
        <>
            {children}
        </>
    )
}

export default layout