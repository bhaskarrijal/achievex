import React from 'react'


export const metadata = {
    title: 'Sign in - AchieveX',
}

const layout = ({ children }) => {
    return (
        <>
            {children}
        </>
    )
}

export default layout